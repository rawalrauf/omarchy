#!/bin/bash

# omarchy:summary=Hibernate the system without triggering hyprlock
# omarchy:group=system
# omarchy:name=hibernate
# omarchy:examples=omarchy system hibernate

touch /tmp/omarchy-hibernating
systemctl hibernate
