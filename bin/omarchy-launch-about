#!/bin/bash

# omarchy:summary=Launch the fastfetch TUI that gives information about the current system.

exec omarchy-launch-or-focus-tui --app-id=org.omarchy.about bash -c 'fastfetch; read -n 1 -s'
