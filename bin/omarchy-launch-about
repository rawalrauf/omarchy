#!/bin/bash

# omarchy:summary=Launch the fastfetch TUI that gives information about the current system.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omarchy.about -e bash -c 'fastfetch; read -n 1 -s'
