#!/bin/bash

# omarchy:summary=Returns the battery percentage remaining as an integer.

upower -i $(upower -e | grep BAT) | awk '
  /energy:/ && !/design/ { energy=$2 }
  /energy-full-design/   { design=$2 }
  END { if (design > 0) printf "%d", (energy/design)*100 }
'
