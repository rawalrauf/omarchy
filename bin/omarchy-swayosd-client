#!/bin/bash

# omarchy:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# omarchy:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(niri msg --json focused-output | jq -r '.name')" "$@"
