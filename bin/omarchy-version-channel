#!/bin/bash

# omarchy:summary=Print the active Omarchy channel based on current git branch

branch=$(git -C "$OMARCHY_PATH" branch --show-current 2>/dev/null)

case "$branch" in
  master|main) echo "stable" ;;
  dev)         echo "dev" ;;
  *)           echo "${branch:-unknown}" ;;
esac
